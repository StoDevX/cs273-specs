# S23
---
assignment: ws8

compilers:
  - &c 'gcc $@ -o $@.exec'

files:
  - file: pipe_test.c
    commands: *c
    tests: 
      - echo -e "Hello!!" | $@.exec
      - echo -e "Another test" | $@.exec
  - file: pipeline_test.c
    commands: *c
    tests: $@.exec

supporting:
  - file: execve_test.c