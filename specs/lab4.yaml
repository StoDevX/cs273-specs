# S23
---
assignment: lab4

compilers:
  - &c 'gcc $@ -o $@.exec'

files:
  - file: lab4.tar
    tests: tar -tvf $@
    options:
      truncate_contents: 2
  - file: lab4-2.tar
    tests: tar -tvf $@
    options:
      truncate_contents: 2
      